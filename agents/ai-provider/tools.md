# AI Provider Agent — Tools

## Herramientas recomendadas
- Ollama.
- OpenRouter dashboard (solo lectura).
- curl con headers desde `.env`.

## MCPs relacionados
- InsForge MCP.

## Comandos de diagnóstico
```bash
ollama list
ollama --version
curl http://localhost:11434/api/tags
```

## Archivos que puede leer
- `~/harnesses/docs/*.md` relacionados con AI.
- `~/harnesses/projects/*/.env.example`

## Archivos que puede modificar con cuidado
- `.env.example`.
- Documentación de providers.

## Archivos prohibidos sin permiso
- `.env` real.
- `~/.config/opencode/opencode.json`.
